require 'spec_helper'

describe GemSamplePompom do
  it 'has a version number' do
    expect(GemSamplePompom::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
